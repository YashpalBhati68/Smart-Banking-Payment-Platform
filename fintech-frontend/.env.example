# Base URL for the Spring Boot backend API.
# In dev, leave as /api so Vite's proxy forwards to http://localhost:8080.
# In production, set to your deployed backend, e.g. https://api.yourbank.com/api
VITE_API_BASE_URL=/api
